# These two values ship inside the static bundle and are public by design.
# Anyone can read them from the deployed JS. That is expected: the publishable
# key only grants what RLS allows, which is why every table has policies and
# every policy has a negative-case test in supabase/tests/rls.sql.
#
# The service_role key must NEVER appear in this file or anywhere in src/.
NEXT_PUBLIC_SUPABASE_URL=https://vockiqvlijtkxvpdttya.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_VrGb2daesMaOAJfIpCqLzg_eR-8JMcb
