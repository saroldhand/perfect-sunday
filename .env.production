# Production builds target GitHub Pages, which serves this project from a
# subpath. Kept in an env file rather than passed on the command line because
# Git Bash on Windows rewrites a leading-slash value into a Windows path.
#
# When a custom domain is bought, blank this line out — that is the whole
# migration.
NEXT_PUBLIC_BASE_PATH=/perfect-sunday
